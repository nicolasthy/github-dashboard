pre-commit:
  parallel: false
  commands:
    biome-check:
      glob: "*.{ts,tsx,js,jsx,json,jsonc}"
      run: bun turbo check
      stage_fixed: true
    typecheck:
      glob: "*.{ts,tsx}"
      run: bun turbo typecheck
